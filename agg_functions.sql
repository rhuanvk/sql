-- traz o maior valor da coluna
select max(coluna) as nome_escolhido
from tabela;

-- traz o menor valor da coluna
select min(coluna) as nome_escolhido
from tabela;

-- traz a media dos valores da coluna
select avg(coluna) as nome_escolhido
from tabela;

-- soma os valores da coluna
select sum(coluna) as nome_escolhido
from tabela;